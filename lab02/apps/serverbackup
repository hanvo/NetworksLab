serverbackup

		// expected = received;
		// for(received = 0; received < expected;) {
		// 	len = recv(conn, buff, (expected - received) 
		// 		< BUFFSIZE ? (expected - received) : BUFFSIZE, 0);
		// 	if(len < 0 ) {
		// 		send_eof(conn);
		// 		return 1;
		// 	}
		// 	(void) write(STDOUT_FILENO, buff, len);
		// 	received += len;
		// }
		// (void) printf("\n");
		// (void) printf(RECEIVED_PROMPT);
		// (void) fflush(stdout);
